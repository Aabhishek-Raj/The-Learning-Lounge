{
  "name": "@repo/docker-dev",
  "private": true,
  "scripts": {
    "dev": "echo 'Skipping docker compose for local dev'",
    "db:reset": "docker compose rm --force --stop postgres && docker compose up -d"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
